## Development Requirements

This project was developed using [vite-plus](https://github.com/voidzero-dev/vite-plus) to manage Node.js and npm versions.

Install `vite-plus` to manage the required Node.js and npm versions, or ensure that they match the `devEngines` requirements in `package.json` if you manage them manually.